@using CampusEats.Client.Models

<div class="grid grid-cols-2 lg:grid-cols-4 gap-4 mb-6">
    <!-- Most Sold -->
    <div class="bg-gradient-to-br from-amber-50 to-orange-50 rounded-xl p-5 border border-amber-200">
        <div class="flex items-center gap-2 mb-3">
            <span class="text-2xl">🏆</span>
            <span class="text-sm font-medium text-amber-700">Most Sold</span>
        </div>
        @if (Items.MostSold != null)
        {
            <p class="text-lg font-bold text-slate-800 truncate">@Items.MostSold.Name</p>
            <p class="text-sm text-amber-700 mt-1">@Items.MostSold.Quantity units</p>
        }
        else
        {
            <p class="text-slate-400">No data</p>
        }
    </div>

    <!-- Least Sold -->
    <div class="bg-gradient-to-br from-slate-50 to-slate-100 rounded-xl p-5 border border-slate-200">
        <div class="flex items-center gap-2 mb-3">
            <span class="text-2xl">📉</span>
            <span class="text-sm font-medium text-slate-600">Least Sold</span>
        </div>
        @if (Items.LeastSold != null)
        {
            <p class="text-lg font-bold text-slate-800 truncate">@Items.LeastSold.Name</p>
            <p class="text-sm text-slate-500 mt-1">@Items.LeastSold.Quantity units</p>
        }
        else
        {
            <p class="text-slate-400">No data</p>
        }
    </div>

    <!-- Morning Bestseller -->
    <div class="bg-gradient-to-br from-yellow-50 to-amber-50 rounded-xl p-5 border border-yellow-200">
        <div class="flex items-center gap-2 mb-3">
            <span class="text-2xl">☀️</span>
            <span class="text-sm font-medium text-yellow-700">Morning Best (6-12)</span>
        </div>
        @if (Items.MorningBestseller != null)
        {
            <p class="text-lg font-bold text-slate-800 truncate">@Items.MorningBestseller.Name</p>
            <p class="text-sm text-yellow-700 mt-1">@Items.MorningBestseller.Quantity units</p>
        }
        else
        {
            <p class="text-slate-400">No data</p>
        }
    </div>

    <!-- Evening Bestseller -->
    <div class="bg-gradient-to-br from-indigo-50 to-purple-50 rounded-xl p-5 border border-indigo-200">
        <div class="flex items-center gap-2 mb-3">
            <span class="text-2xl">🌙</span>
            <span class="text-sm font-medium text-indigo-700">Evening Best (17-22)</span>
        </div>
        @if (Items.EveningBestseller != null)
        {
            <p class="text-lg font-bold text-slate-800 truncate">@Items.EveningBestseller.Name</p>
            <p class="text-sm text-indigo-700 mt-1">@Items.EveningBestseller.Quantity units</p>
        }
        else
        {
            <p class="text-slate-400">No data</p>
        }
    </div>
</div>

@code {
    [Parameter, EditorRequired] public ItemInsights Items { get; set; } = default!;
}